#!/bin/sh

prefix=/usr/local/c-icap-0.4.x

PKGLIBDIR=/usr/local/c-icap-0.4.x/lib/c_icap/
LIBDIR=/usr/local/c-icap-0.4.x/lib/
CONFIGDIR=/usr/local/c-icap-0.4.x/etc/
DATADIR=/usr/local/c-icap-0.4.x/share/c_icap/
#LOGDIR=
SOCKDIR=/var/run/c-icap

INCDIR=/usr/local/c-icap-0.4.x/include
INCDIR2=/usr/local/c-icap-0.4.x/include/c_icap

VERSION=0.4.3

CFLAGS="-D_REENTRANT -g -O2 -Wall -D_FILE_OFFSET_BITS=64"
LIBS="-L$LIBDIR -licapapi"
LDFLAGS=""


usage()
{
   cat <<EOTXT
Usage: c-icap-libicapapi-config [OPTIONS]

Where OPTION one of the following:
   --cflags		print preprocessor and compiler flags
   --libs		print linker flags
   --version		print the c-icap library  version
EOTXT
}


case $1 in
    '--cflags')
        echo $CFLAGS" -I"$INCDIR" -I"$INCDIR2;
        ;;
    '--libs')
       echo $LIBS;
       ;;
    '--version')
       echo $VERSION;
       ;;
    *)
        usage
esac;
